const initalState = [
  {
    "name": "Dopey",
    "age": 3000,
    "height": "3cm",
    "id": 14
  }
]

export function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}